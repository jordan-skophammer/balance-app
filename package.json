{
  "name": "balance_app",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "cd client && react-scripts test",
    "start": "concurrently \"node server.js\" \"cd client && yarn start\"",
    "heroku-postbuild": "cd client && yarn install && yarn run build"
  },
  "author": "Jordan Skophammer <jordan.skophammer@gmail.com>",
  "description": "monthly balance app using react and mongodb",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^4.1.1",
    "cors": "^2.8.5",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.6.3",
    "morgan": "^1.9.1",
    "npm": "^6.10.0"
  },
  "devDependencies": {
    "flow-bin": "^0.102.0",
    "http-proxy-middleware": "^0.19.1"
  }
}
