import React from 'react';
import {Navbar, NavItem, Icon} from 'react-materialize';

const Nav = () => {
    return (
        <Navbar>
            <NavItem>
                <Icon>Home</Icon>
            </NavItem>
        </Navbar>
    )
     
}

export default Nav